# VPN Service Frontend

React + Vite фронтенд приложение для управления VPN сервисом.

## Настройка

Скопируйте файл окружения и добавьте название вашего приложения:

```bash
cp env.example .env
```

Затем отредактируйте файл `.env` и измените значение `VITE_APP_NAME`.